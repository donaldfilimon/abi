[package]
name = "abi-plugins"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "ABI framework plugins: the sixteen bundled plugins, the mod/stub parity invariant, and the plugin manager shared by the CLI and MCP surfaces."

[dependencies]
abi-core = { workspace = true }
abi-foundation = { workspace = true }
abi-telemetry = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
