# =============================================================================
# ABI Instance - Basic Example
# =============================================================================
# A minimal AbiInstance with default settings
apiVersion: abi.io/v1
kind: AbiInstance
metadata:
  name: abi-basic
  namespace: default
spec:
  replicas: 1
