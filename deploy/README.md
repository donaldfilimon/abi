# Deployment Assets

This directory contains baseline deployment resources:

- `docker/` - Docker Compose definitions
- `kubernetes/` - Kubernetes manifests
- `monitoring/` - Prometheus/Grafana templates
- `nginx/` - Reverse proxy configuration
- `scripts/` - Helper scripts
- `sql/` - Database helpers

Review and adapt these files for your environment before production use.
