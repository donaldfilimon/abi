# Personas

Persona definitions used by AI features and deployment presets live in this
folder. Each persona is a TOML file with prompt and policy settings.

- `dev.toml` - Development-friendly defaults
