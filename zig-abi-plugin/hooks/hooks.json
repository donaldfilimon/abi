{
  "PostToolUse": [
    {
      "matcher": "Edit|Write",
      "hooks": [
        {
          "type": "prompt",
          "prompt": "If this edit modified a file matching `src/features/*/mod.zig`, remind the user: 'Remember to update the matching stub.zig with the same public function signatures. Run /zig-abi:check stub-sync to verify.' Only warn if the file path matches that pattern — otherwise say nothing and output empty string."
        }
      ]
    }
  ]
}
