{
  "description": "ABI development guardrails: stub sync warnings, module import validation, format safety, and completion verification",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check the command for this pattern:\nIf the command contains `zig fmt .` (formatting from the repo root), warn: 'Do NOT run `zig fmt .` from the repository root — it walks vendored fixtures with intentionally invalid code. Use `zig fmt --check build.zig build/ src/ tools/ examples/ tests/ bindings/ lang/` to target specific directories instead.'\nOnly warn if matched — otherwise output empty string."
          }
        ]
      },
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check the edit for this pattern:\nIf the file path matches `src/features/*/` AND the edit introduces `@import(\"abi\")`, warn: 'Files inside src/features/ are part of the abi module. Using @import(\"abi\") from within the module causes a circular dependency error. Use relative imports (e.g., @import(\"../../services/shared/mod.zig\")) or @import(\"build_options\") instead.'\nOnly warn if matched — otherwise output empty string."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check the edit for THREE patterns:\n1. If this edit modified a file matching `src/features/*/mod.zig`, remind: 'Remember to update the matching stub.zig with the same public function signatures. Run /zig-abi:check stub-sync to verify.'\n2. If this edit modified a file matching `src/features/*/stub.zig`, remind: 'Ensure the matching mod.zig has the same public function signatures. Run /zig-abi:check stub-sync to verify.'\n3. If this edit introduced `@import(\"../../wdbx/` or `@import(\"../../../wdbx/` or any relative path containing `wdbx/wdbx.zig` from within `src/features/`, warn: 'Use `@import(\"wdbx\")` (named module) instead of a relative path to avoid module conflict — a file can only belong to one Zig module.'\nOnly warn if matched — otherwise output empty string."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Before stopping, provide an advisory checklist of ABI project standards (do NOT block — this is informational only):\n1. If any file in src/features/*/mod.zig was edited this session, was the matching stub.zig also updated? If not, remind the user.\n2. If any Zig files were edited, was `zig fmt --check` run on them? If not, suggest it.\n3. If any CLI commands in tools/cli/commands/ were added or modified, was `refresh-cli-registry` mentioned? If not, remind.\n4. If the task involved build.zig changes, were the is_blocked_darwin guards considered?\nOutput reminders for any unaddressed items, or confirm all checks pass."
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/load-context.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
