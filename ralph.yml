# Ralph Orchestrator Configuration
# Generated by: abi ralph init

cli:
  backend: "plugin_http"

llm:
  backend: "plugin_http"
  fallback: "openai,llama_cpp,mlx,ollama,lm_studio,vllm,plugin_native"
  strict_backend: false
  model: ""
  plugin: "http://localhost:3000"

event_loop:
  prompt_file: "PROMPT.md"
  completion_promise: "LOOP_COMPLETE"
  max_iterations: 5

execution:
  max_iterations: 5
  max_fix_attempts: 2
  require_clean_tree: true

gates:
  per_iteration: "zig build verify-all"
