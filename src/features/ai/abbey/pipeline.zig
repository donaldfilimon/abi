pub const abbey_train = @import("abbey_train.zig");

// Re-exports
// Currently there are no specific types exported from abbey_train in mod.zig, 
// but we include it here to complete the facade structure.
