const gpu_cluster = @import("../gpu_cluster.zig");

pub const ParallelismStrategy = gpu_cluster.ParallelismStrategy;
pub const ModelPartition = gpu_cluster.ModelPartition;
pub const TensorPartition = gpu_cluster.TensorPartition;
