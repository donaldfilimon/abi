//! Owns symbolic lookup structures.

const std = @import("std");

pub const BTreeIndex = struct {
    // TODO: time-range or ordered retrieval
};

pub const HashIndex = struct {
    // TODO: direct id lookup, namespace and user indexes, tag and type indexes
};
