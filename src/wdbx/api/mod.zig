//! Stable boundaries for outside use.

const std = @import("std");

pub const RpcServer = struct {
    // TODO: binary RPC for node communication, HTTP admin or query interface, internal operator APIs
};
