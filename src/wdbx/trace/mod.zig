//! Makes the system inspectable.

const std = @import("std");

pub const TraceLog = struct {
    // TODO: retrieval traces, score provenance, lineage graphs, audit logs for tool use and memory use
};
