//! Developer and operator tooling.

const std = @import("std");

pub const CliHandler = struct {
    // TODO: ingest documents or blocks, query memory, inspect trace output, trigger compaction, create snapshots
};
