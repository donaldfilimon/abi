//! Owns semantic retrieval.

const std = @import("std");

pub const VectorIndex = struct {
    // TODO: embedding storage, distance metrics, quantization, flat/approximate indexes, reranking integration
};
