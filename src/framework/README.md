//! Framework Module Overview
//!
//! High‑level orchestration layer that ties together core, compute, and feature
//! modules. Exposes the public API for library users and coordinates feature
//! initialization based on build flags.
