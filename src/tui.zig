//! Terminal User Interface for the Abi AI Framework

const std = @import("std");

/// Main entry point for TUI mode
pub fn run() !void {
    std.debug.print("TUI mode not yet implemented\n", .{});
}
