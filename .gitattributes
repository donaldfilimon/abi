# Git attributes for Zig projects
# https://git-scm.com/docs/gitattributes

# Set default behavior to automatically normalize line endings
* text=auto

# Zig source files
*.zig text eol=lf
*.zig diff=zig

# Build files
build.zig text eol=lf
build.zig.zon text eol=lf

# Markdown files
*.md text eol=lf

# YAML files
*.{yml,yaml} text eol=lf

# JSON files
*.json text eol=lf

# Shell scripts (Unix)
*.{sh,bash} text eol=lf

# Windows batch files
*.{bat,cmd} text eol=crlf

# PowerShell scripts
*.ps1 text eol=crlf

# Makefiles
Makefile text eol=lf

# Binary files
*.{exe,dll,so,dylib} binary
*.wdbx binary
*.wal binary

# Archives
*.{zip,tar,gz,bz2,xz,7z} binary

# Images
*.{png,jpg,jpeg,gif,svg,ico,bmp} binary

# Large data files
*.{csv,jsonl,parquet} -diff -merge

# Documentation PDFs
*.pdf binary

# Web assets
*.{html,css,js} text eol=lf

# Generated files
zig-cache/ -diff -merge
zig-out/ -diff -merge
*.tmp -diff -merge

# Test artifacts
*_test binary
test_* binary

# Zig tests should remain textual for review
tests/**/*.zig text eol=lf diff=zig
src/tests/**/*.zig text eol=lf diff=zig
