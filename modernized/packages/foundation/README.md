# modernized/packages/foundation

Phase D scaffold pointer — **not** live code.

| | |
|--|--|
| Planned role | See `analysis/src/REIMAGINED_ARCHITECTURE.md` §3 |
| Live path today | `src/foundation/` (HTTP leaves under `src/foundation/http/`) |
| Build | Not wired — use `./build.sh check` on `src/` |

Do not claim this package replaces `src/` until cutover.
