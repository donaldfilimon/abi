# Performance Results

This document summarizes the benchmark outcomes for the ABI framework on a reference hardware configuration. The numbers are generated by `scripts/run_benchmarks.bat` and reflect the latest release **v0.1.1**.

| Benchmark | Ops/sec |
|-----------|---------|
| Framework Init | 175 |
| Logging | 331,960 |
| SIMD Operations | 84,000,000 |
| DB Insert | 68,444 |
| DB Search | 56,563 |
| GPU Availability Check | 183 |

*All values are average throughput measured over multiple runs.*

For detailed per‑backend numbers (Vulkan, Metal, WebGPU, OpenGL, CUDA) see the generated CSV files in `benchmarks/results/`.

