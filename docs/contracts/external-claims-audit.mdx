---
title: "External Claims Audit"
description: "This audit reconciles public collateral claims from the Drive files opened on 2026-05-25 with the current ABI checkout. Treat repo source, Cargo.toml…"
---

This audit reconciles public collateral claims from the Drive files opened on 2026-05-25 with the current ABI checkout. Treat repo source — `Cargo.toml`, `crates/`, `tests/golden/`, `./tools/check.sh`, and this `docs/contracts/` directory — as the source of truth.

## Audited Sources

- `MLAI Infrastructure Components and Specifications` spreadsheet.
- `High-Performance Neural State Management: Technical Analysis of WDBX and Multi-Persona Architectures` Google Doc.

## Repo-Backed Claims

| Claim area | Current repo evidence | Safe external wording |
| --- | --- | --- |
| Toolchain | `rust-toolchain.toml`, `./tools/cargo.sh`, `./tools/check.sh`, README quick start | ABI targets **nightly Rust** via `rust-toolchain.toml`. Always invoke Cargo through `./tools/cargo.sh` (never bare `cargo`). Primary gate: `./tools/check.sh`. Older Zig-pin wording in historical docs is stale. |
| WDBX vector store | `crates/abi-wdbx/src/store.rs`, `hnsw.rs`, `tests/golden/` | WDBX provides an in-process vector store with fixed-capacity padded vectors, HNSW-style indexing, SIMD cosine distance, ordered search-result contract coverage, and disabled-feature stubs. |
| Block history | `crates/abi-wdbx/src/mvcc.rs`, golden / unit tests | WDBX provides SHA-256-linked conversation blocks with snapshot iteration and integrity verification coverage. |
| Spatial records | `crates/abi-wdbx/src/spatial.rs`, store `stats()` | WDBX includes an in-memory 3D spatial index with Euclidean, Manhattan, and cosine-distance searches. |
| AI profiles | `crates/abi-ai/src/router.rs`, `crates/abi-ai/src/lib.rs` | ABI has local Abbey, Aviva, and Abi profile routes selected by deterministic keyword-weighted heuristics, with optional EMA state persistence. |
| Abbey identity contract | `crates/abi-ai/src/identity.rs`, `router.rs`, `incremental.rs`, `docs/spec/abbey-core-identity.mdx` | Abbey is the primary local deterministic profile, Aviva is the direct expert mode, and ABI is the orchestration/governance mode. The preserved Primary Declaration is product direction: visual generation, accessibility adaptation, distributed AI, production multi-host WDBX, blanket security controls, and empirical quality/performance outcomes remain Partial or Proposed as mapped in the canonical spec. |
| Completion persistence | completion helpers + WDBX store paths, contract / golden tests | Completion persistence is opt-in; when WDBX is enabled it records query/response vectors, metadata, and append-linked blocks in a caller-provided store. |
| MCP/CLI | `crates/abi-cli/src/usage.rs`, `crates/abi-mcp/src/handlers.rs`, `stdio.rs`, `http.rs`, golden fixtures | Public CLI commands and MCP tools are small, frozen surfaces guarded by golden/contract tests. MCP stdio is local IPC; loopback HTTP/SSE can require `Authorization: Bearer <token>` via `ABI_MCP_HTTP_TOKEN`, but this is not a production non-loopback exposure claim without TLS/authz/rate-limit review. |
| Accelerator compute | `crates/abi-compute/`, `crates/abi-gpu/src/{adapters,metal_kernels}.rs`, `crates/abi-wdbx/src/v2/index.rs`, workspace tests | `abi-compute` supplies object-safe accelerator contracts, deterministic CPU SIMD, stable top-k, and a five-state compiled/available/initialized/executed/runtime-verified evidence ladder. On supported macOS builds, Metal dot/cosine/norm/batch-cosine execution is locally scoped and marked runtime-verified only after CPU-oracle parity. The CoreML adapter loads and executes a deterministic tiny model under a `.cpuAndNeuralEngine` request, advancing initialized/executed only after its prediction matches the expected output. CoreML does not expose placement through that API, so the adapter does not claim ANE residency or set runtime-verified. CUDA/Vulkan adapters do not have verified runtime execution. Do not infer speedups from availability or compilation. |
| Local model runtime | `crates/abi-model-runtime/`, generated scratch-fixture contract tests | `abi-model-runtime` loads an exact principal-authorized external manifest into an explicitly requested Candle device. Its deterministic `abi-bigram-v1` fixture proves Tokenizers, Safetensors, CPU inference, and locally exercised Metal tensor operations while reporting initialization and execution separately. It is infrastructure evidence, not a production language model, Gemma implementation, quality benchmark, placement profile, speedup, or CUDA runtime claim. No model artifacts are committed. |
| Authenticated worker control plane | `crates/abi-worker/`, offline public contract tests | `abi-worker` validates audience-bound Ed25519 job and cancellation envelopes, derives principal-scoped request identity internally, enforces capabilities/deadlines/leases/quotas/replay bounds, propagates cooperative cancellation, verifies bounded result streams and hashes, and reconstructs append-only job-control state. It reuses the gateway's owner-protected mandatory-mTLS configuration without giving WDBX data-plane storage membership or job-control authority. This is an offline manager and transport-configuration foundation, not an active listener, scheduler, model executor, production certificate lifecycle, deployed cluster, or separate-host proof. |
| Shaders / MLIR | `crates/abi-gpu/src/shaders.rs`, `crates/abi-gpu/src/mlir.rs` | The `shaders` surface validates shader source and emits a checksummed descriptor (`backend="validated-local"`); the `mlir` surface emits a textual ABI-dialect lowering (`backend="textual-local"`). Both report `available=false`: no external shader compiler or MLIR/LLVM toolchain is linked. Do not claim real shader compilation or MLIR/LLVM lowering. |
| Mobile | `crates/abi-gpu/src/mobile.rs`, workspace tests | The `mobile` surface reports compile-target/profile metadata and renders a synthetic text view. `native_dispatch=false` is authoritative: no UIKit/Android runtime, packaged mobile app target, device/emulator execution, or native mobile dispatch is implemented. |
| Connectors | `crates/abi-connectors/`, `docs/contracts/public-api.mdx` | OpenAI, Anthropic, Discord, Grok, and Twilio connectors validate local/live boundaries; live HTTP dispatch is explicit; Discord/Twilio logs redact message and provider-response bodies to metadata. |
| WDBX v2 and local expansion | `crates/abi-wdbx/src/{codecs,fhe,tfhe_demo,v2,cluster}.rs`, `crates/abi-wdbx/src/{codecs,v2,cluster}/`, `crates/abi-compute/`, `crates/abi-cli/src/wdbx/`, `tools/{check_rust_sizes,check_full_fhe}.sh` | The product-facing `VersionedStore` creates v2 and preserves the verified v1 migration/backup, causal-conflict, authenticated-object, rekey, and confirmed-GC boundaries. V2 can export/import the exact encoded committed transaction, preserving object identity and failing closed on gaps, conflicts, tampering, or key mismatch. Deterministic persisted PQ and autoencoder artifacts integrate with segment codecs and record validation/quality metrics; this is not a SOTA or production-quality claim. Accelerator batch search uses the shared `abi-compute` contract and preserves deterministic CPU parity/fallback. Signed membership, rendezvous placement, exact replica quorum/read repair/search fan-out, hash-verified resumable rebalance, and `cluster local-demo` are locally tested single-host behavior, not production separate-host deployment or sharding. DGHV refresh is explicitly secret-key-assisted/educational; optional pinned TFHE-rs demos execute boolean/integer/programmable-bootstrap APIs but have no ABI-independent cryptographic audit or protective-use approval. The Rust size gate enforces 1,000 lines per Rust source and 200 for `abi-cli/src/main.rs`, including untracked non-ignored Rust files. |
| WDBX network gateway | `crates/abi-wdbx-gateway/`, `proto/gateway.proto`, gateway integration tests | Current local source implements all eight bounded bearer-authenticated gRPC methods plus metadata-only WebSocket mutation/query events on a separate explicit listener. Tests exercise plaintext gRPC+WebSocket, TLS+mTLS gRPC+WSS, missing-client-certificate refusal, failed-auth rate budgets, request/key/value/vector/batch/stream/idle limits, hostile/duplicate/null Origin rejection, slow-consumer teardown, permission-checked `O_NOFOLLOW` secret loading, and non-loopback fail-closed policy. `MembershipChange` persists WDBX canonical signed records with a store-local owner-protected keypair, verifies the bounded lineage on reopen, and returns generation/head/tombstone evidence. This remains gateway-local authenticated state—not separate-host consensus, operator-managed cluster membership, hosted/production deployment, DAST validation, or external review. |

## Claims To Remove Or Downgrade

| External claim | Repo status | Replacement wording |
| --- | --- | --- |
| WDBX is a production distributed database with intelligent sharding. | Not proven. The repo now tests signed membership, rendezvous placement, exact quorum replication/read repair, resumable rebalance, authenticated RPC, and a 3–9 node local multi-process demo, but not a production separate-host deployment. | WDBX has exact single-host replication and cluster-control proofs; production multi-host deployment/sharding remains an explicit gap. |
| WDBX proves `12,000 QPS`, `8.2 ms` latency, or `20-30%` lower latency. | Not currently proven by repo source, tests, or benchmark artifacts. | The repo validates functional search/block contracts; publish performance only with fresh benchmark artifacts. |
| WDBX includes AES-256 encryption or RBAC. | False. The v2 product path has opt-in XChaCha20-Poly1305 encryption and Ed25519 signing for transaction and compacted-segment objects, permission-checked key files, signature-required verification, verified generation-replacement rekey, and confirmed generation-local conservative GC. Plaintext v2 remains supported, so product cutover is not a claim that all data is encrypted. | Say only that the v2 path has locally tested opt-in authenticated immutable objects, retained-generation rekey, and active-generation GC. Do not claim AES, RBAC, external audit, or universal encrypted storage. |
| ABI/WDBX is implemented in Swift 6 or Swift 6.2 Span. | Not present in the repo. | ABI/WDBX are Rust nightly workspace crates; macOS builds may link Metal/Foundation/Objective-C for optional GPU DOT paths. |
| ABI is implemented as a Python/JavaScript framework using TensorFlow, PyTorch, PostgreSQL, MongoDB, RabbitMQ, Kafka, Docker, Kubernetes, or Jenkins. | Not currently reflected by this repo's implementation or build. | ABI is a Rust nightly codebase with local connectors, workspace feature crates, build-time plugin registry metadata, and `./tools/check.sh` gates. |
| ABI proves `295x` GPU matmul speedup or `13x` neural-network speedup. | Not currently proven by repo benchmark outputs. | GPU paths report backend capabilities and CPU fallback behavior. |
| WDBX proves `10,000 req/s`, `50 ms` latency, or `95%` accuracy. | Not currently proven by repo tests or benchmark artifacts. | Keep these as future benchmark targets unless a reproducible ABI benchmark artifact exists. |
| Abbey/Aviva/Abi have measured empathy, technical-accuracy, SQuAD, CodeSearchNet, or model-comparison scores. | Not currently proven by repo tests. | The repo has deterministic local routing/profile behavior and validation helpers, not external model-quality benchmarks. |
| The multi-persona system proves `15 kWh/1k` energy use or `25%` efficiency gains. | Not currently measured by repo tests or tooling. | Do not publish energy-efficiency claims without a reproducible measurement artifact. |
| ABI performs dynamic neural persona blending using model-output interpolation formulas. | Not implemented as neural output blending. | ABI uses keyword-weighted profile selection and optional EMA smoothing of route weights. |
| ABI is deployed on H100/A100 clusters, Kubernetes auto-scaling, InfiniBand/NVLink, or decentralized blockchain nodes. | Not currently proven by this repo. | Present those only as external deployment proposals, not current ABI repo capabilities. |
| ABI is certified for GDPR, CCPA, HIPAA, ISO 27001, or compliance-by-design guarantees. | Not currently proven by repo source or tests. | The repo has local constitution checks, connector validation, and POSIX credential-file permission hardening, but no regulatory certification evidence. |

## Reusable Delta For External Artifacts

Use this concise replacement paragraph when updating Drive docs:

> Current ABI repo evidence supports a Rust nightly local AI orchestration framework with deterministic Abbey/Aviva/Abi routing and a v2 WDBX product path with verified migration, causal conflicts, authenticated immutable objects, exact committed-transaction replication, accelerator batch search with CPU parity, deterministic persisted PQ/autoencoder codec artifacts, and single-host exact replica/read-repair/rebalance proofs. Optional macOS Metal numerical execution is locally runtime-verified against a CPU oracle. CoreML tiny-model inference is output-checked under a `.cpuAndNeuralEngine` request, but ANE placement and residency remain unverified; CUDA/Vulkan runtime execution also remains unverified. The DGHV refresh and optional TFHE-rs path are reference/demo evidence without independent cryptographic audit. The repo does not prove production multi-host deployment, hosted/Windows runtime behavior, AES/RBAC or universal encrypted storage, SOTA compression, production cryptography, benchmark speedups, regulatory certification, or external DAST. HawkScan and Semgrep validation were unavailable for this reconciliation.
