# Zig 0.17-dev Refactoring Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refactor ABI framework to align with Zig 0.17-dev (master) standards and fix system-wide parity check failures.

**Architecture:**
1. Update project-wide build system configuration.
2. Address compatibility issues in core modules and connectors.
3. Propagate API changes and fix parity test failures.

**Tech Stack:** Zig 0.17.0-dev (master)

---

### Task 1: Update Build Configuration

**Files:**
- Modify: `build.zig`
- Modify: `build.zig.zon`

- [ ] **Step 1: Update `build.zig.zon` minimum Zig version**

Replace `.minimum_zig_version` with `"0.17.0-dev.0"` (or match current toolchain version).

- [ ] **Step 2: Update `build.zig` to Zig 0.17-dev compatibility**

Ensure `std.Build` methods are consistent with 0.17.0-dev. Check for any breaking changes in `b.option` or `b.standardTargetOptions`.

- [ ] **Step 3: Commit**

```bash
git add build.zig build.zig.zon
git commit -m "refactor(build): update to Zig 0.17-dev"
```

### Task 2: Refactor Discord Connector for Zig 0.17-dev

**Files:**
- Modify: `src/connectors/discord/mod.zig`
- Modify: `src/connectors/discord/rest/core.zig`

- [ ] **Step 1: Audit `src/connectors/discord/mod.zig` for 0.17-dev incompatibilities**

Focus on `std.ArrayListUnmanaged` usage and any other type changes.

- [ ] **Step 2: Audit `src/connectors/discord/rest/core.zig` for 0.17-dev incompatibilities**

Ensure HTTP handling and memory management follow 0.17-dev idioms.

- [ ] **Step 3: Run Parity Test**

Run `./build.sh check-parity`.

- [ ] **Step 4: Commit**

```bash
git add src/connectors/discord/
git commit -m "refactor(discord): update to Zig 0.17-dev"
```

### Task 3: Address Remaining Parity Test Failures

**Files:**
- Modify: `src/common/parity_gate.zig`
- Modify: `src/core/build_validation.zig`

- [ ] **Step 1: Identify and fix remaining systemic parity failures**

Analyze the output of `./build.sh check-parity` and apply fixes to core modules.

- [ ] **Step 2: Commit**

```bash
git add src/common/ src/core/
git commit -m "refactor(core): fix systemic parity failures"
```
