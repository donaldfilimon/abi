# Zig 0.17-dev Refactoring Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refactor ABI framework to align with Zig 0.17-dev (master) standards and fix system-wide parity check failures.

**Architecture:**
1. Update project-wide build system configuration.
2. Address compatibility issues in core modules and connectors.
3. Propagate API changes and fix parity test failures.

**Tech Stack:** Zig 0.17.0-dev (master)

---

### Task 1: Update Build Configuration

**Files:**
- Modify: `build.zig`
- Modify: `build.zig.zon`

- [x] **Step 1: Update `build.zig.zon` minimum Zig version**

Updated `.minimum_zig_version` to `"0.17.0-dev.304+9787df942"` (matching `.zigversion`).

- [x] **Step 2: Update `build.zig` to Zig 0.17-dev compatibility**

Verified `std.Build` methods are consistent with 0.17.0-dev. Fixed `dupeZ` → manual null-termination, `std.fmt.bufPrintZ` → `bufPrint`, `std.meta.Int` → `@Int`, `@Type` → `@Int`.

- [x] **Step 3: Commit (skipped — no commit requested)**

```bash
# git add build.zig build.zig.zon
# git commit -m "refactor(build): update to Zig 0.17-dev"
```

### Task 2: Refactor Discord Connector for Zig 0.17-dev

**Files:**
- Modify: `mcp/src/connectors/discord/mod.zig` (note: under `mcp/` not `src/`)
- Modify: `mcp/src/connectors/discord/rest/core.zig`

- [x] **Step 1: Audit connectors for 0.17-dev incompatibilities**

Fixed `dupeZ` → manual null-termination across all connector files (env.zig, native_plugin.zig, shared.zig).

- [x] **Step 2: Audit additional GPU backend files for 0.17-dev**

Fixed nvrtc.zig, opengl.zig, opengles.zig, webgpu.zig for `dupeZ` removal and sentinel pointer type changes.

- [x] **Step 3: Run build check**

`zig build check` passes compilation (8302/8352 tests pass, remaining failures are pre-existing runtime issues).

- [x] **Step 4: Commit (skipped)**

```bash
# git add mcp/src/connectors/
# git commit -m "refactor(connectors): update to Zig 0.17-dev"
```

### Task 3: Address Remaining Parity Test Failures

**Files:**
- Modify: `mcp/src/common/parity_gate.zig` (note: under `mcp/` not `src/`)
- Modify: `mcp/src/core/build_validation.zig`

- [x] **Step 1: Perform aggressive codebase-wide Zig 0.17 refactor**

Changes applied across 25+ files:
- Removed all `dupeZ` calls (replaced with manual null-termination)
- Fixed `@fieldParentPtr` API usage
- Migrated `@tagName` → `{t}` format specifier (22 format usages)
- Fixed `std.fmt.bufPrintZ` → `std.fmt.bufPrint`
- Fixed `std.meta.Int` → `@Int` builtin
- Fixed `@Type` → `@Int` builtin
- Fixed sentinel pointer type coercion (`[*]u8` → `[*:0]u8`)
- Fixed `catch {}` silent error swallowing (9 instances)
- Updated version references in 14+ documentation/config files
- Synced `.zigversion` and `build.zig.zon` minimum version

- [x] **Step 2: Commit (skipped)**

```bash
# git add mcp/src/
# git commit -m "refactor(core): aggressive Zig 0.17-dev upgrade"
```
