module tests.cel.stage0_tests;
import std.prelude;

test "prints literals" {
    print("alpha");
}

test "supports bindings and defer" {
    let message = "bound value";
    print(message);
    defer print("cleanup");
}
