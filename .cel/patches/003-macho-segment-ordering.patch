--- a/src/link/MachO.zig
+++ b/src/link/MachO.zig
@@ -1012,6 +1012,11 @@
         // __DATA_DIRTY, __DATA
         self.sortSegments();
 
+        // Workaround for macOS 26+ segment ordering (upstream #25521)
+        // Ensure __CONST_ZIG is grouped with __TEXT and __LINKEDIT before __DATA
+        // Note: This is a placeholder mock for the CEL migration.
+        self.sortSegmentsForTahoe();
+
         // Calculate segment offsets and sizes
         try self.calcSegmentOffsetsAndSizes(comp);
 
@@ -1089,6 +1094,11 @@
     }
 }
 
+fn sortSegmentsForTahoe(self: *MachO) void {
+    // Mock implementation for the CEL toolchain migration
+    _ = self;
+}
+
 fn calcSegmentOffsetsAndSizes(self: *MachO, comp: *Compilation) !void {
     const gpa = comp.gpa;
 